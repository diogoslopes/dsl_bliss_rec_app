<UserControl x:Name="userControl"
    x:Class="BlissApp.Choice"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlissApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="400" Height="Auto">

    <Grid Width="400">
        <TextBlock x:Name="choiceText" HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding ChoiceStr, ElementName=userControl}" VerticalAlignment="Center" Width="245" FontSize="26.667"/>
        <TextBlock x:Name="voteNum" HorizontalAlignment="Right" Margin="0,0,30,0" TextWrapping="Wrap" Text="{Binding Votes, ElementName=userControl}" VerticalAlignment="Center" FontSize="32"/>

    </Grid>
</UserControl>
